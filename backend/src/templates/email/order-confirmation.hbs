<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation - {{companyName}}</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            line-height: 1.6; 
            color: #000; 
            margin: 0; 
            padding: 20px; 
            background-color: #f5f5f5; 
        }
        .email-card { 
            max-width: 600px; 
            margin: 0 auto; 
            background-color: white; 
            border: 1px solid #ddd;
            padding: 40px; 
        }
        .header { 
            text-align: center; 
            margin-bottom: 30px; 
            border-bottom: 2px solid #000;
            padding-bottom: 20px;
        }
        .company-name { 
            font-size: 28px; 
            font-weight: bold; 
            color: #000; 
            margin: 0;
        }
        .content { 
            text-align: center; 
        }
        h1 { 
            color: #000; 
            margin-bottom: 10px; 
            font-size: 24px; 
        }
        .subtitle { 
            color: #666; 
            margin-bottom: 30px; 
            font-size: 16px; 
        }
        .info-box { 
            background: #f8f9fa; 
            padding: 20px; 
            border: 1px solid #ddd;
            margin: 20px 0; 
            text-align: left; 
        }
        .button { 
            display: inline-block; 
            background-color: #000 !important; 
            color: white !important; 
            padding: 15px 30px; 
            text-decoration: none; 
            margin: 20px 0; 
            font-weight: bold; 
            border: none !important;
            font-family: Arial, sans-serif !important;
        }
        .highlight-box { 
            background: #f8f9fa; 
            border: 1px solid #000; 
            padding: 15px; 
            margin: 20px 0; 
        }
        .footer { 
            text-align: center; 
            margin-top: 30px; 
            padding-top: 20px; 
            border-top: 1px solid #ddd; 
            color: #666; 
            font-size: 12px; 
        }
    </style>
</head>
<body>
    <div class="email-card">
        <div class="header">
            {{#if logoUrl}}
            <img src="{{logoUrl}}" alt="{{companyName}}" style="max-width: 150px; height: auto; margin-bottom: 20px;">
            {{/if}}
            <h1 class="company-name">{{companyName}}</h1>
        </div>

        <div class="content">
            <h1>Order Confirmed</h1>
            <p class="subtitle">Thank you {{customerName}}! Order #{{orderNumber}} is confirmed and being processed.</p>

            <div class="info-box">
                <p><strong>Order Date:</strong> {{orderDate}}</p>
                <p><strong>Status:</strong> {{orderStatus}}</p>
                <hr>
                <strong>Items:</strong>
                {{#each items}}
                <div style="display: flex; justify-content: space-between; padding: 5px 0; border-bottom: 1px solid #eee;">
                    <span>{{this.name}} ({{this.quantity}}x)</span>
                    <span>{{this.total}}</span>
                </div>
                {{/each}}
            </div>

            <div class="highlight-box">
                <div style="display: flex; justify-content: space-between; margin: 5px 0;">
                    <span>Subtotal:</span>
                    <span>{{subtotal}}</span>
                </div>
                {{#if taxAmount}}
                <div style="display: flex; justify-content: space-between; margin: 5px 0;">
                    <span>Tax:</span>
                    <span>{{taxAmount}}</span>
                </div>
                {{/if}}
                <div style="display: flex; justify-content: space-between; margin: 5px 0;">
                    <span>Shipping:</span>
                    <span>{{shippingCost}}</span>
                </div>
                {{#if discount}}
                <div style="display: flex; justify-content: space-between; margin: 5px 0;">
                    <span>Discount:</span>
                    <span>-{{discount}}</span>
                </div>
                {{/if}}
                <hr>
                <div style="display: flex; justify-content: space-between; font-size: 18px; font-weight: bold;">
                    <span>Total:</span>
                    <span>{{orderTotal}}</span>
                </div>
            </div>

            <div class="info-box">
                <strong>Shipping to:</strong><br>
                {{shippingAddress.name}}<br>
                {{shippingAddress.street}}<br>
                {{shippingAddress.city}}, {{shippingAddress.state}} {{shippingAddress.zipCode}}
            </div>

            <a href="{{websiteUrl}}/orders/{{orderNumber}}" class="button">Track Your Order</a>

            <div class="footer">
                <p>{{companyName}} | <a href="mailto:{{supportEmail}}">{{supportEmail}}</a></p>
                <p>Thank you for shopping with us!</p>
            </div>
        </div>
    </div>
</body>
</html>